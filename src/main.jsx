import React from 'react';
import ReactDOM from 'react-dom/client';
import MealPlanner from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MealPlanner />
  </React.StrictMode>
);
